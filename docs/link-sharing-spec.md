# 링크 공유 기능 기술 명세서

> 작성일: 2026-01-23
> 버전: 2.0 (태그 기반으로 변경)

## 목차
1. [개요](#1-개요)
2. [데이터베이스 스키마](#2-데이터베이스-스키마)
3. [API 설계 (Server Actions)](#3-api-설계-server-actions)
4. [웹 앱 페이지 및 컴포넌트](#4-웹-앱-페이지-및-컴포넌트)
5. [Chrome Extension 구조](#5-chrome-extension-구조)
6. [AI 자동 태그 기능](#6-ai-자동-태그-기능)
7. [구현 순서](#7-구현-순서)

---

## 1. 개요

### 1.1 기능 요약
사용자가 유용한 링크를 저장하고 **태그**로 정리할 수 있는 기능. 개인 링크와 팀 링크를 분리하여 관리하며, 다양한 무료 AI API (Cerebras, Groq, Gemini 등)를 통해 자동 태그 부여 가능.

### 1.2 핵심 요구사항

| 구분 | 설명 |
|------|------|
| **개인 링크** | 본인만 볼 수 있는 링크 |
| **팀 링크** | 팀원 모두가 볼 수 있는 링크 |
| **태그 시스템** | 복수 태그 부여 가능, 개인/팀 태그 별도 관리 |
| **메타데이터** | 메모, 별점(0-5), favicon |
| **AI 자동 태그** | AI API 키 설정 시 URL 붙여넣기 + Enter만으로 자동 저장 (8개 Provider 지원) |
| **수동 태그** | API 키 없으면 태그 선택 후 Enter로 저장 |
| **Chrome Extension** | 현재 탭을 빠르게 저장 (동일한 UX) |
| **북마크 가져오기** | Chrome 북마크 가져오기 (폴더 → 태그 변환) |
| **설정 유지** | 개인/팀 선택 상태가 다음 저장 시에도 유지됨 |

### 1.3 핵심 UX 플로우

**API 키 있는 경우 (자동 모드):**
```
URL 붙여넣기 → Enter → AI가 기존 태그에서 선택 → 자동 저장 완료
```

**API 키 없는 경우 (수동 모드):**
```
URL 붙여넣기 → 태그 선택 → Enter → 저장 완료
```

### 1.4 기존 프로젝트 스택

| 항목 | 현재 패턴 |
|------|----------|
| **프레임워크** | Next.js 16 (App Router) |
| **인증** | NextAuth v5 (JWT 전략, Google OAuth) |
| **DB** | Prisma + PostgreSQL |
| **Server Actions** | `'use server'` + Zod 검증 + `ActionResult<T>` 타입 |
| **UI** | shadcn/ui + Tailwind CSS + lucide-react 아이콘 |
| **폼** | react-hook-form + @hookform/resolvers/zod |

---

## 2. 데이터베이스 스키마

### 2.1 Prisma 스키마 추가

```prisma
// prisma/schema.prisma에 추가

// ============================================
// 링크 공유 모델
// ============================================

enum LinkOwnerType {
  PERSONAL
  TEAM
}

// 태그 모델
model LinkTag {
  id        String        @id @default(cuid())
  name      String
  color     String        @default("#3B82F6")  // 태그 색상
  ownerType LinkOwnerType @map("owner_type")
  
  // 소유자 (PERSONAL일 때 userId 사용, TEAM일 때 teamId 사용)
  userId    String?       @map("user_id")
  user      User?         @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  teamId    String?       @map("team_id")
  team      Team?         @relation(fields: [teamId], references: [id], onDelete: Cascade)
  
  // 태그가 붙은 링크들 (다대다)
  links     LinkTagOnLink[]
  
  createdAt DateTime      @default(now()) @map("created_at")
  updatedAt DateTime      @updatedAt @map("updated_at")
  
  // 같은 소유자 내에서 태그 이름 중복 방지
  @@unique([userId, name, ownerType])
  @@unique([teamId, name, ownerType])
  @@index([userId, ownerType])
  @@index([teamId, ownerType])
  @@map("link_tags")
}

// 링크 모델
model Link {
  id          String        @id @default(cuid())
  url         String
  title       String
  description String?       @db.Text  // 메모
  favicon     String?                 // favicon URL
  rating      Int           @default(0)  // 0-5 별점
  ownerType   LinkOwnerType @map("owner_type")
  
  // 소유자
  userId      String?       @map("user_id")
  user        User?         @relation("PersonalLinks", fields: [userId], references: [id], onDelete: Cascade)
  
  teamId      String?       @map("team_id")
  team        Team?         @relation(fields: [teamId], references: [id], onDelete: Cascade)
  
  // 생성자 (팀 링크의 경우 누가 추가했는지)
  createdById String        @map("created_by_id")
  createdBy   User          @relation("CreatedLinks", fields: [createdById], references: [id])
  
  // 태그들 (다대다)
  tags        LinkTagOnLink[]
  
  createdAt   DateTime      @default(now()) @map("created_at")
  updatedAt   DateTime      @updatedAt @map("updated_at")
  
  @@index([userId, ownerType])
  @@index([teamId, ownerType])
  @@index([rating])
  @@index([createdAt])
  @@map("links")
}

// 링크-태그 연결 테이블 (다대다)
model LinkTagOnLink {
  id        String   @id @default(cuid())
  linkId    String   @map("link_id")
  link      Link     @relation(fields: [linkId], references: [id], onDelete: Cascade)
  tagId     String   @map("tag_id")
  tag       LinkTag  @relation(fields: [tagId], references: [id], onDelete: Cascade)
  
  createdAt DateTime @default(now()) @map("created_at")
  
  @@unique([linkId, tagId])
  @@index([linkId])
  @@index([tagId])
  @@map("link_tag_on_link")
}
```

### 2.2 User 모델 업데이트

```prisma
model User {
  // ... 기존 필드들 ...
  
  // 링크 공유 관련 (추가)
  linkTags       LinkTag[]
  personalLinks  Link[]        @relation("PersonalLinks")
  createdLinks   Link[]        @relation("CreatedLinks")
  
  // AI 자동 태그 설정 (추가)
  aiProvider     String?       @map("ai_provider")   // 'cerebras' | 'groq' | 'gemini' | 'openrouter' | 'together' | 'cohere' | 'glm' | 'mistral'
  aiApiKey       String?       @map("ai_api_key") @db.Text  // 암호화 저장
  aiModel        String?       @map("ai_model")      // 선택된 모델 (선택사항)
  aiAutoTagEnabled Boolean     @default(true) @map("ai_auto_tag_enabled")  // false면 수동 태그 사용
  
  // 링크 저장 설정 (이전 선택 유지)
  linkSavePersonal Boolean     @default(true) @map("link_save_personal")
  linkSaveTeam     Boolean     @default(false) @map("link_save_team")
}
```

### 2.3 Team 모델 업데이트

```prisma
model Team {
  // ... 기존 필드들 ...
  
  // 링크 공유 관련 (추가)
  linkTags       LinkTag[]
  links          Link[]
}
```

---

## 3. API 설계 (Server Actions)

### 3.1 타입 정의

```typescript
// src/types/links.ts

import { LinkOwnerType } from '@prisma/client'

// ============================================
// 태그 타입
// ============================================

export interface LinkTagBasic {
  id: string
  name: string
  color: string
  ownerType: LinkOwnerType
}

export interface LinkTagWithCount extends LinkTagBasic {
  _count: {
    links: number
  }
}

// ============================================
// 링크 타입
// ============================================

export interface LinkWithDetails {
  id: string
  url: string
  title: string
  description: string | null
  favicon: string | null
  rating: number
  ownerType: LinkOwnerType
  tags: LinkTagBasic[]
  createdBy: {
    id: string
    name: string | null
    image: string | null
  }
  createdAt: Date
  updatedAt: Date
}

export interface CreateLinkInput {
  url: string
  title: string
  description?: string
  favicon?: string
  rating?: number
  ownerType: LinkOwnerType
  tagIds: string[]  // 선택된 태그 ID들
}

export interface LinkFilters {
  ownerType?: LinkOwnerType
  tagIds?: string[]  // 선택된 태그들 (AND 조건)
  rating?: number    // 이상인 것만
  search?: string
  startDate?: Date
  endDate?: Date
}

export interface PaginatedLinks {
  links: LinkWithDetails[]
  total: number
  page: number
  perPage: number
  totalPages: number
}

// ============================================
// AI 태그 추천
// ============================================

export interface AITagResult {
  tagIds: string[]
  tagNames: string[]
  confidence: number
  reason: string
}

// ============================================
// Chrome 북마크 가져오기
// ============================================

export interface ChromeBookmarkNode {
  id: string
  title: string
  url?: string
  children?: ChromeBookmarkNode[]
}

// ============================================
// 사용자 설정
// ============================================

export interface LinkSaveSettings {
  savePersonal: boolean
  saveTeam: boolean
}
```

### 3.2 Server Actions - 태그 관리

**파일:** `src/actions/link-tags.ts`

| 함수명 | 설명 | 입력 | 출력 |
|--------|------|------|------|
| `getTags(ownerType)` | 태그 목록 조회 | `'PERSONAL' \| 'TEAM'` | `ActionResult<LinkTagWithCount[]>` |
| `createTag(input)` | 태그 생성 | `{ name, color, ownerType }` | `ActionResult<LinkTagBasic>` |
| `updateTag(id, input)` | 태그 수정 | `id, { name?, color? }` | `ActionResult<LinkTagBasic>` |
| `deleteTag(id)` | 태그 삭제 | `id` | `ActionResult` |

### 3.3 Server Actions - 링크 관리

**파일:** `src/actions/links.ts`

| 함수명 | 설명 |
|--------|------|
| `getLinks(filters, page, perPage)` | 링크 목록 조회 (페이지네이션) |
| `getLinkById(id)` | 링크 단일 조회 |
| `createLink(input)` | 링크 생성 |
| `createLinks(inputs)` | 링크 일괄 생성 (북마크 가져오기용) |
| `updateLink(id, input)` | 링크 수정 |
| `deleteLink(id)` | 링크 삭제 |
| `quickSaveLink(url, ownerTypes)` | 빠른 저장 (AI 자동 태그) |

### 3.4 Server Actions - AI 태그

**파일:** `src/actions/ai-tags.ts`

| 함수명 | 설명 |
|--------|------|
| `suggestTags(url, title, ownerType)` | AI로 태그 추천 (기존 태그에서 선택) |
| `saveGroqApiKey(apiKey)` | Groq API 키 저장 |
| `getGroqApiKey()` | Groq API 키 조회 (마스킹) |
| `deleteGroqApiKey()` | Groq API 키 삭제 |

### 3.5 Server Actions - 설정

**파일:** `src/actions/link-settings.ts`

| 함수명 | 설명 |
|--------|------|
| `getLinkSaveSettings()` | 저장 설정 조회 (개인/팀 체크 상태) |
| `updateLinkSaveSettings(settings)` | 저장 설정 업데이트 |

### 3.6 Server Actions - 북마크 가져오기

**파일:** `src/actions/import-bookmarks.ts`

| 함수명 | 설명 |
|--------|------|
| `importChromeBookmarks(bookmarks, ownerType, folderToTagMapping)` | Chrome 북마크 가져오기 (폴더명 → 태그 변환) |

---

## 4. 웹 앱 페이지 및 컴포넌트

### 4.1 라우트 구조

```
src/app/(dashboard)/links/
├── page.tsx                    # 메인 링크 목록 페이지
├── loading.tsx                 # 로딩 상태
├── tags/
│   └── page.tsx               # 태그 관리 페이지
└── import/
    └── page.tsx               # Chrome 북마크 가져오기 페이지
```

### 4.2 컴포넌트 구조

```
src/components/links/
├── quick-link-input.tsx       # URL 입력 + 빠른 저장 (핵심 컴포넌트)
├── link-card.tsx              # 링크 카드 컴포넌트
├── link-list.tsx              # 링크 목록
├── link-dialog.tsx            # 링크 추가/수정 다이얼로그
├── link-filters.tsx           # 필터 UI (태그, 별점, 검색)
├── tag-select.tsx             # 태그 다중 선택 컴포넌트
├── tag-badge.tsx              # 태그 뱃지 컴포넌트
├── tag-dialog.tsx             # 태그 추가/수정 다이얼로그
├── tag-list.tsx               # 태그 목록 (관리 페이지용)
├── star-rating.tsx            # 별점 입력 컴포넌트
├── owner-type-checkbox.tsx    # 개인/팀 체크박스 (설정 유지)
└── bookmark-import-wizard.tsx # 북마크 가져오기 위자드
```

### 4.3 메인 페이지 UI

```
┌─────────────────────────────────────────────────────────────────┐
│ 링크 공유                         [북마크 가져오기] [태그 관리] │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 🔗 URL 붙여넣기 후 Enter (API 키 있으면 자동 저장)         │ │
│ │ [https://                                              ]   │ │
│ │                                                             │ │
│ │ [✓] 개인  [ ] 팀                     ← 이전 설정 유지      │ │
│ └─────────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────────┤
│ 필터: [개인 ▼]  태그: [React] [Node.js] [+]  별점: [⭐3+ ▼]    │
│                                                   [검색...]     │
├─────────────────────────────────────────────────────────────────┤
│ 총 47개 링크                                                    │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 🔗 React 공식 문서                                   [개인] │ │
│ │    https://react.dev                                        │ │
│ │    [React] [공식문서] [Frontend]                            │ │
│ │    ⭐⭐⭐⭐⭐  "핵심 레퍼런스"                    2시간 전   │ │
│ │                                        [수정] [삭제]        │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 🔗 Tailwind CSS                                       [팀]  │ │
│ │    https://tailwindcss.com                                  │ │
│ │    [CSS] [UI]                                               │ │
│ │    ⭐⭐⭐⭐☆  by 홍길동                           1일 전    │ │
│ └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### 4.4 빠른 링크 입력 컴포넌트 (핵심)

**통합 UI (API 키 유무 상관없이 동일):**
```
┌─────────────────────────────────────────────────────────────┐
│ 🔗 URL 붙여넣기                                             │
│ [https://react.dev                                      ]   │
│                                                             │
│ 태그: [태그 선택 ▼]  [React] [Frontend] ✕                  │  ← 수동 선택 가능
│                                                             │
│ [✓] 개인  [✓] 팀                                           │
│                                                             │
│ [▶ AI 자동 태그]  [저장]                                    │  ← 둘 다 가능
└─────────────────────────────────────────────────────────────┘
```

**동작 방식:**
- **[태그 선택 ▼]**: 클릭 시 태그 선택 모달 열림
- **[▶ AI 자동 태그]**: API 키 있으면 AI가 태그 자동 선택 + 저장 (없으면 버튼 비활성화)
- **[저장]**: 선택된 태그로 저장
- **Enter**: API 키 있으면 AI 자동 태그, 없거나 태그 선택됨 → 바로 저장

### 4.5 태그 선택 모달 (작은 크기, 뒤로 필터링 보임)

```
┌─────────────────────────────────────────────────────────────────┐
│ 링크 공유                                    [+ 링크 추가]      │
├─────────────────────────────────────────────────────────────────┤
│ 필터: [개인 ▼]  [태그 선택 ▼]  [별점 ▼]       [검색...]        │
│                   │                                             │
│ ┌─────────────────┴───────────────┐                             │
│ │ 태그 필터              [X]      │  ← 작은 모달 (드롭다운형)   │
│ ├─────────────────────────────────┤                             │
│ │ 🔍 [검색...]                    │                             │
│ ├─────────────────────────────────┤                             │
│ │ [React ✓] [Frontend ✓]         │  ← 클릭으로 토글            │
│ │ [Node.js] [Backend]            │                             │
│ │ [CSS] [공식문서 ✓]              │                             │
│ │ [TypeScript] [Python]          │                             │
│ ├─────────────────────────────────┤                             │
│ │ 선택: 3개           [초기화]    │                             │
│ └─────────────────────────────────┘                             │
├─────────────────────────────────────────────────────────────────┤
│ 총 12개 링크 (필터 적용됨)                      ← 실시간 반영   │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 🔗 React 공식 문서                                          │ │
│ │    [React] [공식문서] [Frontend]                            │ │
│ └─────────────────────────────────────────────────────────────┘ │
│ ... 필터링된 링크들 ...                                         │
└─────────────────────────────────────────────────────────────────┘
```

**태그 모달 특징:**
- **작은 크기**: 드롭다운 형태로 뒤의 링크 목록이 보임
- **실시간 필터링**: 태그 클릭 시 즉시 링크 목록 업데이트
- **다중 선택**: 클릭으로 여러 태그 토글 (AND 조건)
- **검색**: 태그 이름으로 검색
- **외부 클릭 시 닫힘**

### 4.6 링크 수정 다이얼로그

```
┌─────────────────────────────────────────┐
│ 링크 수정                           [X] │
├─────────────────────────────────────────┤
│ URL                                     │
│ [https://react.dev                  ]   │
│                                         │
│ 제목                                    │
│ [React 공식 문서                    ]   │
│                                         │
│ 태그: [태그 선택 ▼]                     │  ← 클릭 시 모달
│       [React] [공식문서] [Frontend] ✕   │  ← 선택된 태그
│                                         │
│ 메모                                    │
│ [핵심 레퍼런스                      ]   │
│                                         │
│ 별점                                    │
│ ⭐⭐⭐⭐⭐                               │
│                                         │
│                         [취소] [저장]   │
└─────────────────────────────────────────┘
```

### 4.7 태그 관리 페이지

```
┌─────────────────────────────────────────────────────────────┐
│ 태그 관리                                      [+ 태그 추가] │
├─────────────────────────────────────────────────────────────┤
│ [개인 태그] [팀 태그]                                       │
├─────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ [React]        12개 링크              [수정] [삭제]     │ │
│ │ [Node.js]       8개 링크              [수정] [삭제]     │ │
│ │ [Frontend]      5개 링크              [수정] [삭제]     │ │
│ │ [Backend]       3개 링크              [수정] [삭제]     │ │
│ │ [공식문서]       7개 링크              [수정] [삭제]     │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 4.8 설정 페이지 - AI 자동 태그 섹션

```
┌─────────────────────────────────────────────────────────────┐
│ 🤖 AI 자동 태그 설정                                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ AI Provider 선택 (가격순)                                   │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │  Provider        │ 회당 가격  │ 무료 한도  │ 속도       │ │
│ │ ─────────────────┼───────────┼───────────┼─────────── │ │
│ │ ○ Cerebras       │ ₩0 무료   │ 1M 토큰/일 │ ⚡ 최고속  │ │
│ │ ○ Groq           │ ₩0 무료   │ 500K/일   │ ⚡ 고속    │ │
│ │ ○ Google Gemini  │ ₩0 무료   │ 1000회/일 │ 보통      │ │
│ │ ○ OpenRouter     │ ₩0 무료   │ 50회/일   │ 모델별    │ │
│ │ ○ Together AI    │ ~₩0.3/회  │ $25 크레딧│ 보통      │ │
│ │ ○ Cohere         │ ₩0 무료   │ 1000회/월 │ 보통      │ │
│ │ ○ GLM (Z.AI)     │ ~₩0.1/회  │ 25M/30일  │ 보통      │ │
│ │ ○ Mistral        │ ₩0 무료   │ 제한적    │ 보통      │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ 선택: [Cerebras ▼]                                         │
│                                                             │
│ 💡 API 키 발급: https://cloud.cerebras.ai                  │
│                                                             │
│ API Key                                                     │
│ [                                                   ]       │
│                                                             │
│ ─────────────────────────────────────────────────────────── │
│                                                             │
│ [ ] 자동 태그 기본 사용                                     │
│     체크 시: URL 입력 + Enter → AI 자동 태그 + 저장         │
│     체크 해제 시: 수동으로 태그 선택 후 저장                │
│                                                             │
│ ✅ API 키가 있어도 수동으로 태그를 선택할 수 있습니다       │
│                                                             │
│ [저장]                                           [키 삭제]  │
└─────────────────────────────────────────────────────────────┘
```

**가격 계산 기준:**
- 태그 자동 분류 1회 = 약 400 토큰 (프롬프트 300 + 응답 100)
- 무료 한도 내에서는 모두 ₩0
- Together AI: $0.0002/1K 토큰 ≈ ₩0.3/회
- GLM: ¥0.1/1M 토큰 ≈ ₩0.1/회 (무료 기간 이후)

### 4.9 네비게이션 업데이트

`src/components/layout/sidebar.tsx`:

```typescript
import { Link2 } from 'lucide-react'

const navItems = [
  { href: '/', label: '대시보드', icon: LayoutDashboard },
  { href: '/transactions', label: '거래', icon: Receipt },
  { href: '/links', label: '링크 공유', icon: Link2 },  // 추가
  { href: '/categories', label: '카테고리', icon: Tags },
  { href: '/settings', label: '설정', icon: Settings },
]
```

---

## 5. Chrome Extension 구조

### 5.1 프로젝트 구조

```
chrome-extension/
├── manifest.json
├── popup/
│   ├── popup.html
│   ├── popup.css
│   └── popup.js
├── background/
│   └── service-worker.js
├── utils/
│   ├── api.js
│   └── storage.js
└── icons/
    ├── icon-16.png
    ├── icon-32.png
    ├── icon-48.png
    └── icon-128.png
```

### 5.2 manifest.json (Manifest V3)

```json
{
  "manifest_version": 3,
  "name": "KPI Dashboard - 링크 저장",
  "version": "1.0.0",
  "description": "KPI Dashboard에 링크를 빠르게 저장하세요",
  
  "permissions": [
    "activeTab",
    "storage",
    "bookmarks"
  ],
  
  "host_permissions": [
    "https://your-domain.com/*",
    "http://localhost:3000/*"
  ],
  
  "action": {
    "default_popup": "popup/popup.html",
    "default_icon": {
      "16": "icons/icon-16.png",
      "32": "icons/icon-32.png",
      "48": "icons/icon-48.png",
      "128": "icons/icon-128.png"
    }
  },
  
  "background": {
    "service_worker": "background/service-worker.js",
    "type": "module"
  },
  
  "icons": {
    "16": "icons/icon-16.png",
    "32": "icons/icon-32.png",
    "48": "icons/icon-48.png",
    "128": "icons/icon-128.png"
  }
}
```

### 5.3 Extension Popup UI

**Extension Popup UI:**
```
┌─────────────────────────────────────┐
│ 🔗 링크 저장                    ⚙️  │  ← 설정 아이콘
├─────────────────────────────────────┤
│ 📄 React – A JavaScript library...  │
│    https://react.dev                │
├─────────────────────────────────────┤
│ 태그: [태그 선택 ▼]                 │  ← 클릭하면 모달
│       [React] [Frontend] ✕          │  ← 선택된 태그 (삭제 가능)
├─────────────────────────────────────┤
│ [✓] 개인  [✓] 팀                    │  ← 이전 설정 유지
├─────────────────────────────────────┤
│ [✓] Chrome 북마크에도 추가          │
│     📁 [북마크바 > 개발 ▼]          │
├─────────────────────────────────────┤
│ [▶ AI 자동 태그] [저장]             │  ← 수동/자동 선택 가능
└─────────────────────────────────────┘
```

**Extension 설정 (⚙️ 클릭 시):**
```
┌─────────────────────────────────────┐
│ ⚙️ Extension 설정              [X]  │
├─────────────────────────────────────┤
│ [✓] 클릭 시 바로 저장               │  ← 활성화하면 팝업 없이 즉시 저장
│     (AI 자동 태그 사용)             │
│                                     │
│ 기본 저장 위치                      │
│ [✓] 개인  [ ] 팀                    │
│                                     │
│ [ ] Chrome 북마크에도 추가          │
│     📁 [북마크바 ▼]                 │
├─────────────────────────────────────┤
│ [저장]                              │
└─────────────────────────────────────┘
```

**"클릭 시 바로 저장" 활성화 시:**
- Extension 아이콘 클릭 → 팝업 없이 즉시 저장
- AI 자동 태그 + 설정된 저장 위치 사용
- 저장 완료 시 뱃지 표시 (✓)

**태그 선택 모달 (Extension):**
```
┌─────────────────────────────────────┐
│ 태그 선택                      [X]  │
├─────────────────────────────────────┤
│ 🔍 [검색...]                        │
├─────────────────────────────────────┤
│ [React ✓] [Frontend ✓] [Node.js]   │
│ [CSS] [Backend] [공식문서 ✓]        │
│ [TypeScript] [Python] [AI]          │
├─────────────────────────────────────┤
│                           [완료]    │
└─────────────────────────────────────┘
```

### 5.4 확장 기능 - 메모/별점 (선택적 펼침)

```
┌─────────────────────────────────────┐
│ 🔗 링크 저장                        │
├─────────────────────────────────────┤
│ 📄 React – A JavaScript library...  │
│    https://react.dev                │
├─────────────────────────────────────┤
│ [✓] 개인  [✓] 팀                    │
├─────────────────────────────────────┤
│ [▼ 추가 옵션]                       │  ← 클릭하면 펼침
│ ┌─────────────────────────────────┐ │
│ │ 메모                            │ │
│ │ [                            ]  │ │
│ │                                 │ │
│ │ 별점  ⭐⭐⭐☆☆                  │ │
│ └─────────────────────────────────┘ │
├─────────────────────────────────────┤
│ [✓] Chrome 북마크에도 추가          │
│     📁 [북마크바 ▼]                 │
├─────────────────────────────────────┤
│ [          저장          ]          │
└─────────────────────────────────────┘
```

### 5.5 인증 플로우 (Cookie 기반)

```
1. 사용자가 웹앱에서 Google 로그인
2. NextAuth가 JWT 세션 쿠키 설정
3. Extension은 host_permissions으로 동일 도메인 쿠키 접근
4. API 호출 시 credentials: 'include'로 쿠키 자동 전송
5. 웹앱 미로그인 시 "로그인이 필요합니다" 안내
```

### 5.5 설정 유지 (chrome.storage)

```javascript
// 저장
await chrome.storage.local.set({
  linkSettings: {
    // 저장 위치
    savePersonal: true,
    saveTeam: false,
    
    // 크롬 북마크
    addToChrome: true,
    chromeFolderId: 'bookmark_bar',
    
    // 바로 저장 (클릭 즉시)
    instantSave: false,  // true면 Extension 클릭 시 팝업 없이 바로 저장
    
    // AI 자동 태그
    useAutoTag: true,    // false면 API 키 있어도 수동 태그 사용
  }
})

// 불러오기
const { linkSettings } = await chrome.storage.local.get('linkSettings')
```

### 5.7 Extension API 엔드포인트

**라우트:**
```
src/app/api/extension/
├── auth/check/route.ts    # GET - 인증 상태 + API 키 존재 여부
├── tags/route.ts          # GET - 태그 목록 (개인+팀)
├── links/route.ts         # POST - 링크 생성
├── links/quick/route.ts   # POST - 빠른 저장 (AI 자동 태그)
└── settings/route.ts      # GET/POST - 저장 설정
```

**응답 예시 (auth/check):**
```json
{
  "authenticated": true,
  "userId": "user_123",
  "teamId": "team_456",
  "hasGroqApiKey": true,
  "settings": {
    "savePersonal": true,
    "saveTeam": false
  }
}
```

---

## 6. AI 자동 태그 기능

### 6.1 동작 방식

1. **사용자가 AI Provider 선택 + API 키 설정**
2. **URL 붙여넣기 + Enter**
3. **서버에서 URL 메타데이터 추출** (제목, 설명)
4. **선택한 AI Provider 호출** → 기존 태그 목록에서 적합한 태그 선택
5. **자동으로 링크 저장** + 선택된 태그 연결
6. **완료 알림**

### 6.2 무료 LLM API 제공자 목록

| 순위 | 제공자 | 발급 링크 | 무료 한도 | 속도 | 신용카드 | 추천 |
|------|--------|----------|----------|------|---------|------|
| 🥇 | **Cerebras** | https://cloud.cerebras.ai | **1M 토큰/일**, 30 RPM | **2000-3000 tok/s** | ❌ 불필요 | ⭐⭐⭐⭐⭐ |
| 🥈 | **Groq** | https://console.groq.com/keys | 500K 토큰/일, 30 RPM | 280+ tok/s | ❌ 불필요 | ⭐⭐⭐⭐⭐ |
| 🥉 | **Google Gemini** | https://aistudio.google.com/app/apikey | 15 RPM, 1000 요청/일 | 보통 | ❌ 불필요 | ⭐⭐⭐⭐ |
| 4 | **OpenRouter** | https://openrouter.ai/settings/keys | 50 요청/일 (무료 모델 18+개) | 모델별 | ❌ 불필요 | ⭐⭐⭐⭐ |
| 5 | **Together AI** | https://api.together.xyz | $25 무료 크레딧 | 보통 | ❌ 불필요 | ⭐⭐⭐⭐ |
| 6 | **Cohere** | https://dashboard.cohere.com | 1000 요청/월 | 보통 | ❌ 불필요 | ⭐⭐⭐ |
| 7 | **GLM (Z.AI)** | https://z.ai | 25M 토큰 (30일 한정) | 보통 | ❌ 불필요 | ⭐⭐⭐ |
| 8 | **Mistral** | https://console.mistral.ai | ~1 RPS (실험용) | 보통 | ⚠️ **필요** | ⭐⭐ |

### 6.3 제공자별 상세 정보

#### 🥇 Cerebras (최고 추천)
- **발급**: https://cloud.cerebras.ai
- **무료 한도**: 1M 토큰/일, 30 RPM, 14,400 요청/일
- **추천 모델**: `llama-3.3-70b`
- **속도**: **2000-3000 tok/s** (GPU 대비 15-20배 빠름)
- **특징**: 초고속 추론, OpenAI 호환 API

#### 🥈 Groq
- **발급**: https://console.groq.com/keys
- **무료 한도**: 500K 토큰/일, 30 RPM
- **추천 모델**: `llama-3.3-70b-versatile`
- **속도**: 280+ tok/s
- **특징**: 안정적, 빠른 응답

#### 🥉 Google Gemini
- **발급**: https://aistudio.google.com/app/apikey
- **무료 한도**: 15 RPM, 250K TPM, 1000 요청/일
- **추천 모델**: `gemini-2.5-flash`
- **특징**: 1M 토큰 컨텍스트, 신용카드 불필요

#### 4. OpenRouter
- **발급**: https://openrouter.ai/settings/keys
- **무료 한도**: 50 요청/일 (무료 모델), 1000 요청/일 ($10 충전 시)
- **무료 모델**: Llama 3.3, Gemini 2.0, DeepSeek R1, GLM 4.5 등 18+개
- **특징**: 여러 제공자 통합, 모델 선택 가능

#### 5. Together AI
- **발급**: https://api.together.xyz
- **무료 한도**: 가입 시 $25 무료 크레딧
- **추천 모델**: `meta-llama/Llama-3.3-70B-Instruct-Turbo`
- **특징**: 200+ 오픈소스 모델 지원

#### 6. Cohere
- **발급**: https://dashboard.cohere.com
- **무료 한도**: 1000 API 요청/월
- **추천 모델**: `command-a-03-2025`
- **특징**: 상용 불가 (개발/테스트용)

#### 7. GLM (Z.AI / 智谱AI)
- **발급**: https://z.ai (해외) / https://open.bigmodel.cn (중국)
- **무료 한도**: 25M 토큰 (가입 후 30일 한정)
- **추천 모델**: `glm-4-flash`
- **특징**: 중국 AI, 글로벌 접근 가능

### 6.4 설정 UI

```
┌─────────────────────────────────────────────────────────────┐
│ 🤖 AI 자동 태그 설정                                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ 💡 무료 API 키 발급받기 (클릭하면 새 탭에서 열림)           │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 🥇 Cerebras (최고 추천)                                 │ │
│ │    https://cloud.cerebras.ai                           │ │
│ │    무료: 1M 토큰/일, 초고속 2000+ tok/s                 │ │
│ │                                                         │ │
│ │ 🥈 Groq                                                 │ │
│ │    https://console.groq.com/keys                       │ │
│ │    무료: 500K 토큰/일, 고속 280+ tok/s                  │ │
│ │                                                         │ │
│ │ 🥉 Google Gemini                                        │ │
│ │    https://aistudio.google.com/app/apikey              │ │
│ │    무료: 15 RPM, 1M 컨텍스트                            │ │
│ │                                                         │ │
│ │ 📋 더 많은 무료 API 보기...                             │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ AI Provider                                                 │
│ [Cerebras ▼]                                               │
│                                                             │
│ API Key                                                     │
│ [                                                   ]       │
│                                                             │
│ ✅ API 키가 설정되면 URL만 붙여넣고 Enter로 자동 저장됩니다 │
│    AI가 기존 태그 중에서 적합한 태그를 자동으로 선택합니다  │
│                                                             │
│ [저장]                                                      │
└─────────────────────────────────────────────────────────────┘
```

### 6.5 지원 Provider 타입 (DB 스키마)

```prisma
// User 모델 업데이트
model User {
  // ... 기존 필드들 ...
  
  // AI 자동 태그 설정
  aiProvider     String?   @map("ai_provider")  // 'cerebras' | 'groq' | 'gemini' | 'openrouter' | 'together' | 'cohere' | 'glm'
  aiApiKey       String?   @map("ai_api_key") @db.Text  // 암호화 저장
  aiModel        String?   @map("ai_model")  // 선택된 모델 (선택사항)
}
```

### 6.6 AI 프롬프트

```typescript
const prompt = `
당신은 링크 분류 전문가입니다. 주어진 링크에 적합한 태그를 기존 태그 목록에서 선택해주세요.

## 링크 정보
- URL: ${url}
- 제목: ${title}
- 설명: ${description || '없음'}

## 사용 가능한 태그 (이 중에서만 선택)
${tags.map(t => `- ${t.name} (id: ${t.id})`).join('\n')}

## 규칙
1. 반드시 위 태그 목록에서만 선택하세요
2. 1~5개의 태그를 선택하세요
3. 가장 관련성 높은 태그를 우선 선택하세요
4. 적합한 태그가 없으면 빈 배열을 반환하세요

## 응답 형식 (JSON만 출력)
{
  "tagIds": ["id1", "id2"],
  "reason": "선택 이유 (한 문장)"
}
`
```

### 6.7 Provider별 API 설정

```typescript
// src/lib/ai-providers.ts

export type AIProvider = 'cerebras' | 'groq' | 'gemini' | 'openrouter' | 'together' | 'cohere' | 'glm'

interface ProviderConfig {
  name: string
  baseUrl: string
  defaultModel: string
  authHeader: (apiKey: string) => Record<string, string>
}

export const AI_PROVIDERS: Record<AIProvider, ProviderConfig> = {
  cerebras: {
    name: 'Cerebras',
    baseUrl: 'https://api.cerebras.ai/v1/chat/completions',
    defaultModel: 'llama-3.3-70b',
    authHeader: (key) => ({ 'Authorization': `Bearer ${key}` }),
  },
  groq: {
    name: 'Groq',
    baseUrl: 'https://api.groq.com/openai/v1/chat/completions',
    defaultModel: 'llama-3.3-70b-versatile',
    authHeader: (key) => ({ 'Authorization': `Bearer ${key}` }),
  },
  gemini: {
    name: 'Google Gemini',
    baseUrl: 'https://generativelanguage.googleapis.com/v1beta/models',
    defaultModel: 'gemini-2.5-flash',
    authHeader: (key) => ({ 'x-goog-api-key': key }),
  },
  openrouter: {
    name: 'OpenRouter',
    baseUrl: 'https://openrouter.ai/api/v1/chat/completions',
    defaultModel: 'meta-llama/llama-3.3-70b-instruct:free',
    authHeader: (key) => ({ 'Authorization': `Bearer ${key}` }),
  },
  together: {
    name: 'Together AI',
    baseUrl: 'https://api.together.xyz/v1/chat/completions',
    defaultModel: 'meta-llama/Llama-3.3-70B-Instruct-Turbo',
    authHeader: (key) => ({ 'Authorization': `Bearer ${key}` }),
  },
  cohere: {
    name: 'Cohere',
    baseUrl: 'https://api.cohere.ai/v1/chat',
    defaultModel: 'command-a-03-2025',
    authHeader: (key) => ({ 'Authorization': `Bearer ${key}` }),
  },
  glm: {
    name: 'GLM (Z.AI)',
    baseUrl: 'https://open.bigmodel.cn/api/paas/v4/chat/completions',
    defaultModel: 'glm-4-flash',
    authHeader: (key) => ({ 'Authorization': `Bearer ${key}` }),
  },
}
```

### 6.8 API 호출 코드 (멀티 Provider 지원)

```typescript
// src/actions/ai-tags.ts

import { auth } from '@/lib/auth'
import { prisma } from '@/lib/prisma'
import { AI_PROVIDERS, AIProvider } from '@/lib/ai-providers'

export async function suggestTags(
  url: string,
  title: string,
  ownerType: 'PERSONAL' | 'TEAM'
): Promise<ActionResult<AITagResult>> {
  const session = await auth()
  if (!session?.user) return { success: false, error: 'Unauthorized' }
  
  // 사용자 AI 설정 가져오기
  const user = await prisma.user.findUnique({
    where: { id: session.user.id },
    select: { aiProvider: true, aiApiKey: true, aiModel: true }
  })
  
  if (!user?.aiProvider || !user?.aiApiKey) {
    return { success: false, error: 'AI API 키가 설정되지 않았습니다' }
  }
  
  const provider = AI_PROVIDERS[user.aiProvider as AIProvider]
  if (!provider) {
    return { success: false, error: '지원하지 않는 AI Provider입니다' }
  }
  
  // 태그 목록 가져오기
  const tags = await prisma.linkTag.findMany({
    where: ownerType === 'PERSONAL' 
      ? { userId: session.user.id, ownerType }
      : { teamId: session.user.teamId!, ownerType },
    select: { id: true, name: true }
  })
  
  if (tags.length === 0) {
    return { success: false, error: '태그가 없습니다. 먼저 태그를 추가해주세요.' }
  }
  
  // AI API 호출 (OpenAI 호환 형식)
  const response = await fetch(provider.baseUrl, {
    method: 'POST',
    headers: {
      ...provider.authHeader(user.aiApiKey),
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      model: user.aiModel || provider.defaultModel,
      messages: [{ role: 'user', content: buildPrompt(url, title, tags) }],
      temperature: 0.1,
      max_tokens: 200,
    }),
  })
  
  if (!response.ok) {
    const error = await response.text()
    console.error('AI API Error:', error)
    return { success: false, error: 'AI 태그 추천에 실패했습니다' }
  }
  
  const data = await response.json()
  const content = data.choices?.[0]?.message?.content
  
  if (!content) {
    return { success: false, error: 'AI 응답을 파싱할 수 없습니다' }
  }
  
  // JSON 파싱
  const result = parseAIResponse(content, tags)
  
  return { success: true, data: result }
}
```

---

## 7. 구현 순서

### Phase 1: 기본 인프라 (1일)

| 순서 | 작업 | 파일 |
|------|------|------|
| 1-1 | Prisma 스키마 업데이트 | `prisma/schema.prisma` |
| 1-2 | 마이그레이션 실행 | `npx prisma migrate dev` |
| 1-3 | 타입 정의 | `src/types/links.ts` |

### Phase 2: 백엔드 API (3-4일)

| 순서 | 작업 | 파일 |
|------|------|------|
| 2-1 | 태그 CRUD Server Actions | `src/actions/link-tags.ts` |
| 2-2 | 링크 CRUD Server Actions | `src/actions/links.ts` |
| 2-3 | AI 태그 Server Actions | `src/actions/ai-tags.ts` |
| 2-4 | 설정 Server Actions | `src/actions/link-settings.ts` |
| 2-5 | 북마크 가져오기 Actions | `src/actions/import-bookmarks.ts` |
| 2-6 | Extension API 엔드포인트 | `src/app/api/extension/*` |
| 2-7 | CORS 설정 | `next.config.ts` |

### Phase 3: 웹 UI (5-7일)

| 순서 | 작업 | 파일 |
|------|------|------|
| 3-1 | 공통 컴포넌트 | `tag-badge`, `tag-select`, `star-rating` |
| 3-2 | 빠른 입력 컴포넌트 | `quick-link-input.tsx` (핵심) |
| 3-3 | 링크 컴포넌트 | `link-card`, `link-list`, `link-dialog` |
| 3-4 | 메인 페이지 | `/links/page.tsx` |
| 3-5 | 태그 관리 페이지 | `/links/tags/page.tsx` |
| 3-6 | 북마크 가져오기 페이지 | `/links/import/page.tsx` |
| 3-7 | 설정 페이지 AI 섹션 | `/settings/page.tsx` 수정 |
| 3-8 | 네비게이션 업데이트 | `sidebar.tsx` |

### Phase 4: Chrome Extension (3-4일)

| 순서 | 작업 | 파일 |
|------|------|------|
| 4-1 | Extension 기본 구조 | `manifest.json`, 아이콘 |
| 4-2 | Popup HTML/CSS | `popup.html`, `popup.css` |
| 4-3 | Popup JS - API 연동 | `popup.js` |
| 4-4 | 태그 선택 UI | `popup.js` |
| 4-5 | 크롬 북마크 폴더 선택 | `popup.js` |
| 4-6 | 설정 저장 | `utils/storage.js` |
| 4-7 | Background Worker | `service-worker.js` |

### 의존성 그래프

```
Phase 1 ──► Phase 2 ──► Phase 3
                   └──► Phase 4 (Phase 2 완료 후 병렬 가능)
```

### 예상 총 기간

- **최소**: 2주 (풀타임, 병렬 작업)
- **일반**: 3주

---

## 부록: 체크리스트

### 보안
- [ ] AI API 키 암호화 저장 (모든 Provider)
- [ ] Extension API CORS 설정
- [ ] 입력값 Zod 검증
- [ ] URL sanitization

### 성능
- [ ] 태그 목록 클라이언트 캐싱
- [ ] 링크 목록 페이지네이션 (20개/페이지)
- [ ] 검색 debounce (300ms)
- [ ] Favicon lazy loading

### UX
- [ ] 개인/팀 체크박스 설정 DB 저장 (User 모델)
- [ ] Extension에서도 설정 동기화
- [ ] 로딩 상태 표시 (AI 태그 추천 중...)
- [ ] 에러 메시지 표시 (toast)
- [ ] 성공 피드백 (저장 완료!)

### 태그 관련
- [ ] 태그 색상 커스터마이징
- [ ] 태그 사용 횟수 표시
- [ ] 자주 쓰는 태그 상단 표시
- [ ] 태그 검색 기능
